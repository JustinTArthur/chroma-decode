# Examples are plain-C consumer programs that prove the public ABI is usable
# from C. They are not installed. Populated incrementally as the library gains
# real functionality.
#
# When functionality is wired up, each example becomes an `executable(...)`
# linked against libchromadec.
