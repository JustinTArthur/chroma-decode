# Test suite. Grows as the library gains functionality.
#
# Unit tests use Catch2 (single-header). Integration tests generate fixtures
# via encode-orc (cloned and built in CI) and compare YUV planes to golden
# output from the legacy ld-chroma-decoder binary built at commit f39e59e18.
