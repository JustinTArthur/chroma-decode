# Format preset DATA tables, per the CVBS file format specification.
# This subdir build file is intentionally empty: the sources are pulled
# into the top-level chromadec_sources list in src/meson.build so they
# link into the same target.
